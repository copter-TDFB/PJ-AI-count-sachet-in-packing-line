__pycache__/
*.pyc
build/
dist/
dist_release/
*.spec
update_tmp/
launcher.log
*_openvino_model/
*.zip
app_backup/
crop_config.json
*.pt
snapshots/
invoices/
สูตร upload เข้า githup.txt

# Office lock files / build previews / scratch (2026-07-02)
~$*
kernel.errors.txt
manual_preview/
manual-html-preview.png

# pm-bridge task packets (local orchestration only)
.pm-bridge/

# Claude Code local state (task worktrees, session config)
.claude/

# pm-bridge: packet/state dirs and executor scratch output. The PM commits
# with `git add -A` before any review, so unignored scratch lands in the PR.
.worktrees/
.codex/
.opencode/

# Odoo credentials — per-machine, never committed. This repo is public: the app reads
# them from OS env vars, this .env (dev only), or the in-app settings dialog.
.env
.env.*

# superpowers subagent scratch (review diffs contain whatever the diff touched).
# Only a nested .gitignore protected this before — belt and braces.
.superpowers/

# .env.example is a committed template with no real values
!.env.example
